vector<int> arr;
long long i;
accumulate(arr.begin(), arr.end()-1, 0); // type of initail value deciding type of final value
accumulate(arr.begin(), arr.end()-1, 0.0);
accumulate(arr.begin(), arr.end()-1, i);
